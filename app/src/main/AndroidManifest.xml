<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.ricarad.app.daydayanswer">
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_PHONE_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/logo"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".activity.LoginActivity"
                    android:launchMode="singleTask">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".activity.RegisterActivity"  android:launchMode="singleTask"/>
        <activity android:name=".administrator.AdministratorActivity"  android:launchMode="singleTask"/>
        <activity android:name=".administrator.QuestionActivity"  android:launchMode="singleTask"/>
        <activity android:name=".administrator.AddQuestionActivity"  android:launchMode="singleTask"/>
        <activity android:name=".normaluser.NormalUserActivity"  android:launchMode="singleTask"/>
        <activity android:name=".normaluser.AnswerQuestionActivity" android:launchMode="singleTask"/>
        <activity android:name=".normaluser.GradeActivity" android:launchMode="singleTask"/>
    </application>

</manifest>